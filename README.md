cd path/to/your-folder     # <- Change to where your files actually are
git init
git remote add origin https://github.com/Ali-Sharif-Ivey/orange-grits-cafe.git
git add .
git commit -m "Add full Orange Grits Café build"
git branch -M main
git push -u origin main --force
